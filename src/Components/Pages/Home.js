import React from 'react';

function Home(props) {
    return (
        <div>
            <h2>This is Home Page</h2>
            <button className='btn btn-primary'>button</button>
        </div>
    );
}

export default Home;